# Session 2026-08-27

## Requests

1. **Initial setup**: Frontend only. TypeScript, React. Single page with 4 panels. Upper-right panel: "picker" with tiled tag balloons in lexicographical order inside a scrollable div, with a filter input above. Bottom-left panel: "list" with vertical rows of tag subsets as balloons with delete buttons. Balloons from the picker can be dragged into the list (onto an existing row or below the last row to create a new one). Rows have a delete button, a slider to change position, checkboxes for multiple selection with ctrl/shift+click support, and a color from a palette of 8 colors.

2. **Replace row slider with drag handle**: Change the row slider for a widget that when dragged can change the ordering of the rows, so the user can drag the rows up and down in the list.

3. **Smooth drag reordering**: Make the draggable widget smoother, when dragging make the row floating.

4. **Rows move apart for floating row**: Make the rest of the rows also move to accommodate the floating row. No blue line between rows, instead the dragged row goes between the rows smoothly.

5. **Replace "Del" button with rubbish bin icon**: Change the "Del" button for a rubbish bin.

6. **White outline on rubbish bin**: Make the rubbish bin icon have a white outline.

7. **Selection behavior change**: Forget about the ctrl key. When a row is clicked, toggle its checkbox and make it the "active" one. When shift+clicking another row, copy the active row's checked state to the range.

8. **Select all / Unselect all buttons**: Add two buttons to the top of the list: "Select all" and "Unselect all", separately enabled based on whether there is still anything to select/unselect.

9. **Fix multi-line row scroll bug**: Rows with multiple lines were breaking when scrolled, being resized to a single line although their contents still occupied 2 lines.

10. **Drop zone fills remaining space**: Make the "Drop here" area occupy the rest of the panel, and at least one row at the bottom.

11. **Fix downward drag stiffness**: Dragging a row downwards was stiff; the calculation was off. Dragging upwards worked fine.

12. **Fix floating row layout for multi-line rows**: Multi-line rows were becoming single-line rows when dragged because the checkbox and delete buttons were missing from the floating preview.

13. **Add tag delete buttons to floating row**: The floating row was also missing the delete buttons on the tags.

14. **Fix floating row left alignment**: The dragged row was sticking to the left edge and not accounting for the margin of the list.

15. **Positional colors**: Make the colors be positional (based on row index) and not insertion-order.

16. **Unique tags**: Make tags unique so when one is dragged from the picker, it disappears from the picker and goes into the desired row.

17. **Drag tags between rows**: Make it possible to drag tags inside a row to another row.

18. **Fix tag drop between rows**: Dropping a tag from one row into another or into the "drop here" zone was not changing the tag location (effectAllowed/dropEffect mismatch fix).

19. **Auto-delete empty rows**: When a row is emptied, delete it automatically.

20. **Multi-select in picker**: Make it possible in the picker to use the ctrl key to select multiple tags. If the user clicks away from the panel, forget the selection.

21. **Show all tags when dragging multiple**: When dragging multiple tags, all should show in the drag preview, not just one.

22. **Marquee selection in picker**: Make it possible in the picker to use the mouse to draw a rectangle for multiple tag selection.

23. **Fix accidental row drag from text selection**: When text was selected in the list after dragging a row, clicking on empty space in a row caused it to drag all its contents.

24. **Create .gitignore file**.

25. **Add customary .gitignore patterns** for this type of project.

26. **Create README.md** with instructions on how to start.

27. **Create session markdown** with all requests from this session.

28. **Tags should not be removed from picker**: Tags should not be removed from the picker so they can be picked multiple times for multiple list rows.

29. **Clear picker selection after drop**: When the tag set is created (dropped), the picker selection should clear. Fixed Ctrl+click after drag re-selecting the tag.

30. **Fix first Ctrl+click not working in picker**: The first Ctrl+click was not working because the browser started a drag instead of firing the click. Prevented drag when Ctrl is held on an unselected tag.

31. **Fix first Ctrl+click for second tag set**: After dropping a selection and starting a new one, the first Ctrl+click didn't work because the `didDrag` flag was still set from the previous drag. Fixed by resetting it asynchronously in `onDragEnd`.

32. **Update session markdown** with new requests.
