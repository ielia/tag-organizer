# Session 2026-09-24

## Requests

1. **Which directory does Claude use** for instructions and MCP servers?

2. **Create a reasonable CLAUDE.md file**, based on the discussion recorded in
   `session-2026-08-27.md`.

3. **Rewrite CLAUDE.md as directives**: the first version read like a README describing the
   project rather than a set of instructions for Claude. Wipe it and initialize a customary
   CLAUDE.md.

4. **New session file per conversation**: in the Housekeeping section, add that a new
   `session-<date>.md` must be started every time a new conversation begins on this project.

5. **Hands off git**: add a directive that Claude must never do anything with git — version
   control is managed by the human alone.

6. **Hands off README.md**: add a directive never to modify `README.md` unless explicitly
   asked.

7. **Ask before installing**: add a directive never to install dependencies without asking.

8. **Bind dev server to 0.0.0.0**: change the configuration so the dev server listens on
   `0.0.0.0` instead of `localhost`.

9. **Fix the Vite config-loader warning** about ESM syntax in a file loaded as CommonJS.

10. **Fix `crypto.randomUUID is not a function`** in the Chrome console when dropping tags
    to create a new row.

11. **"Clear selection" button in the picker**: add a button to the picker pane, styled like
    the ones in the list pane, that clears the tag selection there. Only that one button.

12. **Clear button on the picker filter**: add an "x" button — the same one used on the tags
    in the list pane — to the picker's filter input, to clear the text easily.

13. **Softer hover on the filter clear button**: make its hover highlight just lighter
    instead of red.

14. **Drop the Ctrl requirement in the picker**: clicking a tag selects it, and touching a
    tag on a mobile phone selects it too.

15. **Pointer-based tag drag for mobile**: add a drag path so tags can be moved into and
    between rows on touch devices, where HTML5 drag-and-drop never fires.

16. **Mobile hint in the picker**: add very short explanatory text above the picker panel,
    shown on mobile.

17. **Hint placement and responsive layout**: move the "tap to select / hold to drag" text
    below the filter, and make the page responsive so panes stack vertically on a narrow
    screen.

18. **Responsive layout not applying**: the panes still rendered as a 2x2 grid on a narrow
    screen, with only a sliver of the left panes visible.

19. **Picker action buttons**: move "Clear selection" to a line between the filter and the
    hint, and add "Add to selected rows" (copies the selected tags into the checked list
    rows) and "Add to new row" (creates a row with the selected tags). All three on one line.

20. **Horizontal overflow issues** caused by an over-long tag: the row background covered
    only the initially visible part of the row, the horizontal scrollbar was much thicker
    than the vertical one, and the corner between the two scrollbars stayed white.

21. **Equal row widths**: rows were being sized to their own content width; they should all
    share one width (height may still follow content).

22. **Rows shrinking in height**: once there were enough rows to fill the container, the
    rows compressed vertically instead of keeping their content height and scrolling.

23. **Cap the number of rows**: limit the list to 8 rows, keeping the colours in a list and
    deriving the row limit from that list's length.

24. **Stray clicks clearing the picker selection**: clicking or tapping outside a tag wiped
    the selection; only the "Clear selection" button should do that.

25. **Picker button layout**: rename "Add to new row" to "As new row" and put it first,
    followed by "Add to selected rows", both left-aligned; right-align "Clear selection".

26. **Tag ellipsis behind a config flag**: cap tag width and ellipsise the overflow, driven
    by a `TAG_ELLIPSIS` configuration variable, defaulting to '100%'.

27. **Tooltips for truncated tags**: when `TAG_ELLIPSIS` is not null, show the full tag text
    as a tooltip, in both the picker and the list.

28. **Finger-sized buttons on mobile**: make "As new row", "Add to selected rows", "Clear
    selection", "Select all" and "Unselect all" large enough to tap without precision.

29. **Rounder mobile buttons**: double the corner radius of the action buttons on mobile.

30. **Lighter button colour**: lighten the action buttons' colour on all layouts.

31. **Desktop hint**: add a very small hint for desktop, like the mobile one, explaining the
    selection methods.

32. **Sticky header and options sidebar**: add a sticky header with a hamburger menu on the
    left that opens a floating sidebar, holding a "Manageable tag size" switch that toggles
    `TAG_ELLIPSIS` between null (off) and '100%' (on).

33. **Dark mode option**: add a "Dark mode" option to the sidebar with a ternary switch
    choosing between yes, no and system.

34. **Dark mode labels**: change the dark mode options from Yes/No to On/Off.

35. **Icon buttons for dark mode**: replace the dark mode labels with a crescent moon, a sun
    and a screen (desktop) or phone (mobile), narrower, right-aligned on the same line as the
    "Dark mode" label, keeping the capsule shape.

36. **Reorder dark mode options**: put the system option in the middle.

37. **Default to system**: make the system option the default dark mode setting.

38. **Match control heights**: the dark mode button array looked slightly shorter than the
    "Manageable tag size" switch.

39. **Persist to localStorage**: remember the hamburger options and the list rows across
    reloads.

40. **Persist row selection** as well.

41. **Row reorder scrolls the page on mobile**: dragging the reorder handle moved the whole
    list; it should stay static while a row is being reordered.

42. **Auto-scroll while reordering**: when a dragged row passes the top or bottom of the
    list, scroll the list slowly in that direction.

43. **Slower auto-scroll** while reordering rows.

44. **Auto-scroll speed** raised to half the original value.

45. **Auto-scroll speed** restored to the original.
