export const PALETTE = [
  '#FF6B6B', // red
  '#FFD93D', // yellow
  '#6BCB77', // green
  '#4D96FF', // blue
  '#C27AFF', // purple
  '#FF9F45', // orange
  '#45D0FF', // cyan
  '#FF6EB4', // pink
];

/** One row per colour: rows are coloured by position, so the palette sets the ceiling. */
export const MAX_ROWS = PALETTE.length;
